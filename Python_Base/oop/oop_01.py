# 对象(object)
# --Python是一门面向对象的语言
# --一切皆对象！
# 对象结构
# --对象三种数据
# ----id唯一性（解析器生成，对象的内存地址）
# ----type类型（python是一门强类型的语言，对象一旦创建类型便不能修改）
# ----value值（可变对象，不可变对象）

## 变量和对象
    # -对象并没有直接存储到变量中，在Python中变量更像是给对象起了一个别名
    # -变量中存储的不是对象的值，而是对象的id(内存地址)